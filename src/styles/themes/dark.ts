import { defaultTheme } from './defaults'

export default {
  ...defaultTheme,
  title: 'dark',
  colors: {
    ...defaultTheme.colors,
    primary: '#DCDCDC',
    secondary: '#FFFFFF',
    primaryText: '#000',
    secondaryText: '#333333'
  }
}
