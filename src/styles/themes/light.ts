import { defaultTheme } from './defaults'

export default {
  ...defaultTheme,
  title: 'light',
  colors: {
    ...defaultTheme.colors,
    primary: '#00181d',
    secondary: '#002a2e',
    primaryText: '#fdfdfd',
    secondaryText: '#aecfd2'
  }
}
